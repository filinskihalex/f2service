extend ../../includes/_template.pug 

append config
        - var myTitle = 'Reload Nginx Внутри Контейнера'
        - var myDesc = 'Docker: Reload Nginx Внутри Контейнера'
        - var mykeywords = 'Docker: Reload Nginx Внутри Контейнера , перезагрузка службы внутри docker'
        - var csspatch = '../../css/'
        - var icopatch = '../../icons/'
        - var htmlpatch = '../'

append content
    .content
        p Если хотите обновить какие-либо настройки в Nginx или обновить SSL-сертификаты, чтобы изменения применились необходимо сделать reload.
        p Если Nginx запущен внутри Docker-контейнера, вам будет интересно узнать, как сделать reload без простоя или обрыва соединений.
        p Лучшим способом сказать Nginx внутри Docker-контейнера о необходимости перечитать настройки — это с помощью команды docker container exec отправить Nginx сигнал reload.
        .wrap_code
            .p На всякий случай проверьте корректность синтаксиса настроек в Nginx:
            .code
                | docker exec nginx_cont nginx -t
        .wrap_code
            .p Отправьте Nginx внутри Docker-контейнера сигнал reload:
            .code
                | docker exec nginx_cont -s reload
